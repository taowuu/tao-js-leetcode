/**
 * @param {number} n
 * @return {number}
 */
var fib = function(n) {
    // 自底向上 test
    let arr = [0, 1]
    for(let i = 2; i < n; i++) {
        a[i] = a[i - 1] + a[i - 2]
    }
    return a[i]
};