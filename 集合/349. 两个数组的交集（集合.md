```javascript
/**
 * @param {number[]} nums1
 * @param {number[]} nums2
 * @return {number[]}
 */
var intersection = function(nums1, nums2) {
    // 返回集合 过滤存在 nums2 中的值
    return [...new Set(nums1)].filter(n => nums2.includes(n))
};
```